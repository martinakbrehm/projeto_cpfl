# Diretório de arquivos de lote da macro CPFL.
# Arquivos gerados/lidos pelo ciclo de automação:
#   lote_pendente.csv  — gerado pelo passo 03 (ETL extraction)
#   lote_meta.json     — metadados do lote (macro_id ↔ CPF+UC)
#   resultado_lote.csv — gerado pela macro Selenium
#   arquivo/           — histórico arquivado com timestamp
